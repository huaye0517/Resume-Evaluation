# 简历评估系统 - 部署使用说明

## 系统简介
这是一个智能简历评估工具，可以帮助您批量评估候选人的简历与岗位的匹配度。

## 第一步：安装 Python（仅需一次）

### 1.1 下载 Python
- 打开浏览器访问：https://www.python.org/downloads/
- 点击黄色按钮 "Download Python 3.12.x"
- 等待下载完成

### 1.2 安装 Python
1. 双击下载的安装包（文件名类似 `python-3.12.x.exe`）
2. **重要**：勾选底部的 "Add Python.exe to PATH"（打勾）
3. 点击 "Install Now"
4. 等待安装完成，点击 "Close"

## 第二步：获取系统文件

### 方式 A：使用压缩包（推荐）
1. 从同事处获取 `简历评估系统.zip` 文件
2. 右键点击 → "解压到当前文件夹"
3. 会生成一个文件夹 `简历评估系统`

### 方式 B：从网盘下载
1. 下载完整的项目文件夹
2. 放到桌面或任意位置

## 第三步：配置 API 密钥（重要）

1. 打开项目文件夹，找到 `.env` 文件
2. 右键 → "打开方式" → 选择"记事本"或"Notepad"
3. 找到 `DEEPSEEK_API_KEY=` 这一行
4. 在等号后面粘贴您的 API 密钥（请联系同事获取）
5. 保存文件（Ctrl+S），关闭记事本

**注意**：没有这个密钥，系统只能解析简历，无法进行智能评估。

## 第四步：启动系统

### 方法一：双击启动（推荐）
1. 进入项目文件夹
2. 双击 `start.bat`（图标是齿轮形状）
3. 首次启动会安装依赖，请耐心等待（约 1-3 分钟）
4. 看到 "服务启动成功" 的提示后，浏览器会自动打开

### 方法二：如果双击启动失败
1. 在文件夹地址栏输入 `cmd`，按回车
2. 在黑色窗口中输入：`start.bat`，按回车
3. 按提示操作

## 第五步：使用系统

### 访问系统
- 浏览器会自动打开：http://127.0.0.1:5000
- 如果浏览器没自动打开，手动在浏览器地址栏输入：`127.0.0.1:5000`

### 基本操作流程
1. **输入岗位描述（JD）**：
   - 可以粘贴文字，或上传 JD 文件（PDF/Word/txt）

2. **上传简历**：
   - 点击"上传简历"按钮
   - 支持批量选择多个简历文件
   - 支持格式：PDF、Word(.docx)、txt

3. **开始评估**：
   - 点击"开始评估"按钮
   - 等待评估完成（每份简历约 10-30 秒）

4. **查看结果**：
   - 评估结果会显示匹配度分数
   - 可以查看每个候选人的详细分析

5. **导出 Excel**：
   - 点击"导出 Excel"按钮
   - 评估结果会保存为 Excel 文件

## 常见问题

### Q1: 双击 start.bat 后窗口闪一下就关闭了？
**解决方法**：
1. 确认 Python 安装时勾选了 "Add to PATH"
2. 重新安装 Python 并勾选该选项

### Q2: 提示 "pip 不是内部或外部命令"？
**解决方法**：
1. 重新安装 Python
2. 安装时务必勾选 "Add Python to PATH"
3. 安装完成后重启电脑

### Q3: 浏览器打不开 127.0.0.1:5000？
**解决方法**：
1. 查看黑色命令窗口是否显示 "服务启动成功"
2. 确认窗口没有被关闭
3. 手动输入 `http://localhost:5000` 尝试

### Q4: 评估结果很慢或报错？
**解决方法**：
1. 检查网络连接是否正常
2. 检查 `.env` 文件中的 API 密钥是否正确
3. 同时评估的简历数量建议不超过 20 份

### Q5: 如何停止服务？
**解决方法**：
1. 点击命令窗口，按 `Ctrl + C`
2. 或直接关闭命令窗口

## 文件夹说明

```
简历评估系统/
├── start.bat          # 启动文件（双击运行）
├── .env               # 配置文件（填写 API 密钥）
├── app.py             # 程序主文件（无需修改）
├── requirements.txt   # 依赖列表（无需修改）
├── templates/         # 页面模板（无需修改）
├── static/            # 样式文件（无需修改）
└── ...                # 其他程序文件
```

## 技术支持

如遇到问题，请联系：
- 同事姓名：_________
- 联系方式：_________

## 更新记录

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2026/06/03 | 初始版本 |
